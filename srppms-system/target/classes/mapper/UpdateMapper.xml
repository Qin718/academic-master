<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.system.mapper.UpdateMapper">
    <select id="getListNoId">
        select title, content, time, version
        from sys_update
    </select>
    <select id="getList" resultType="com.example.system.domain.vo.UpdateVo">
        select *
        from sys_update
    </select>
    <insert id="insertUpdateId">
        insert into sys_update (id, title, time, version, name, content)
        values (#{update.id}, #{update.title}, #{update.time}, #{update.version}, #{update.name}, #{update.content})
    </insert>
</mapper>
