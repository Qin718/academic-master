<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.system.mapper.ScoreMapper">
    <insert id="insertScoreList">
        insert into sys_score (id, project_id, user_id, score) VALUES
        <foreach collection="list" item="item" separator=",">
            (#{item.id},#{item.projectId},#{item.userId},#{item.score})
        </foreach>
    </insert>
    <insert id="insertScore" keyColumn="id" keyProperty="id" useGeneratedKeys="true">
        insert into sys_score (project_id, user_id, score)
        VALUES (#{projectId}, #{userId}, #{score})
    </insert>
</mapper>
