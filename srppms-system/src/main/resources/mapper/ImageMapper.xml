<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.system.mapper.ImageMapper">
    <insert id="insertImage" keyColumn="id" keyProperty="id" useGeneratedKeys="true">
        insert into sys_image (image) value (#{image.image})
    </insert>
</mapper>
