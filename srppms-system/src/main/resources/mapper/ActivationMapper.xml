<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.system.mapper.ActivationMapper">
    <insert id="insertActivation" keyColumn="id" keyProperty="id" useGeneratedKeys="true">
        insert into sys_activation (user_id, all_activation, page_view, add_item, score_project, login, process)
        values (#{userId}, #{allActivation}, #{pageView}, #{addItem}, #{scoreProject}, #{login}, #{process})
    </insert>
</mapper>
