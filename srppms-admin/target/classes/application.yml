srppms:
    name: 科研项目管理系统
    author: zzz
    email: 123@
dataBase:
    # 检查数据库
    check: true
    # 创建数据库数据
    create:
        createFriendList: false
        createScoreList: false
        createProcessList: false

spring:
    datasource:
        username: qrz
        password: 030718
        # 改一下自己的数据库密码
        database: srppms
        driver-class-name: com.mysql.cj.jdbc.Driver
        url: jdbc:mysql://127.0.0.1:3306/+${spring.datasource.database}+?serverTimezone=GMT%2B8&useUnicode=true&autoReconnect=true&allowMultiQueries=true&userSSL=false&characterEncoding=UTF8

    main:
        allow-circular-references: true
        allow-bean-definition-overriding: true
    mvc:
        pathmatch:
            matching-strategy:
                ANT_PATH_MATCHER
    redis:
        host: localhost
        port: 6379
        database: 1
        password:
        timeout: 1000s
    servlet:
        multipart:
            enabled: true
            max-file-size: 10MB # 设置单个文件的大小为10M
            max-request-size: 100MB # 设置总上传的数据大小为50M
    profiles:
        active: mail

server:
    port: 8888
    tomcat:
        uri-encoding: UTF-8
        accept-count: 1000
        threads:
            max: 800
            min-spare: 100
# 日志管理
logging:
    level:
        root: info
        com.example: debug
    config: classpath:logback.xml

############## Sa-Token 配置 (文档: https://sa-token.cc) ##############
sa-token:
    # token名称 (同时也是cookie名称)
    token-name: saToken
    # token有效期，单位s 默认30天, -1代表永不过期
    timeout: 2592000
    # token临时有效期 (指定时间内无操作就视为token过期) 单位: 秒
    activity-timeout: -1
    # 是否允许同一账号并发登录 (为true时允许一起登录, 为false时新登录挤掉旧登录)
    is-concurrent: true
    # 在多人登录同一账号时，是否共用一个token (为true时所有登录共用一个token, 为false时每次登录新建一个token)
    is-share: true
    # token风格
    token-style: uuid
    # 是否输出操作日志
    is-log: false